export class Hokage {
   public name: string;
   public hokageNumber: number;
   public sageMode: boolean;

   constructor(name: string, hokageNumber: number, sageMode: boolean) {
     this.name = name;
     this.hokageNumber = hokageNumber;
     this.sageMode = sageMode;
   }
}
